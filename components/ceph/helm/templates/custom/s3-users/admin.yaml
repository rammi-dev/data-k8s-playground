# S3 admin user — bucket management and CLI debugging
# Secret: rook-ceph-object-user-s3-store-admin (AccessKey, SecretKey)
# Used by: build.sh for bucket creation only (not injected into Dremio)
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: admin
spec:
  store: s3-store
  displayName: "S3 Admin User"
  capabilities:
    user: "*"
    bucket: "*"
