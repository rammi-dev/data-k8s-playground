apiVersion: v2
name: rook-ceph-operator
description: Rook-Ceph operator for the data playground
type: application
version: 1.0.0
appVersion: "v20.2"

# Rook Operator chart — tracks the upstream version
# Cluster CRDs are in manifests/ (not Helm)
dependencies:
  - name: rook-ceph
    version: "v1.19.1"
    repository: "https://charts.rook.io/release"
