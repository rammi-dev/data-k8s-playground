apiVersion: v2
name: dremio-playground
description: Dremio data lakehouse for the data playground
type: application
version: 1.0.0
appVersion: "26.1.3"

# Chart source: oci://quay.io/dremio/dremio-helm v3.2.3
# Extracted locally at: helm/dremio/
#
# Install with:
#   helm upgrade --install dremio ./helm/dremio \
#     -n dremio -f ./helm/dremio/values.yaml -f ./helm/values-overrides.yaml
#
# Dependencies (managed by dremio chart):
#   - dremio-mongodb-operator-helm (oci://quay.io/dremio)
#   - nats (https://nats-io.github.io/k8s/helm/charts/)
#   - dremio-opensearch-operator-helm (oci://quay.io/dremio)
