apiVersion: v2
name: airflow-playground
description: Apache Airflow for the data playground
type: application
version: 1.0.0
appVersion: "3.1.7"

# Dependencies on official Apache Airflow Helm chart
# Run: helm dependency update
dependencies:
  - name: airflow
    version: "1.18.0"
    repository: "https://airflow.apache.org"
