# Milvus Overrides — empty
# All configuration is now in values.yaml
# S3 credentials (accessKey/secretKey) are injected by build.sh via --set
#
# This file exists for future environment-specific tweaks.
# To use: helm upgrade --install milvus . -f values.yaml -f values-overrides.yaml
